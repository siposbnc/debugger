/// <reference types="vite/client" />

// Injected by `define` in vite.config.ts from package.json.
declare const __APP_VERSION__: string;
// Build timestamp (UTC), set when vite build runs.
declare const __BUILD_TIME__: string;
